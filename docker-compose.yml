services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: nexum
      POSTGRES_PASSWORD: nexum
      POSTGRES_DB: nexum
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: